apiVersion: v1
kind: Pod

metadata:
  name: taint-demo
  namespace: kuberesue

spec:
  nodeSelector:
    workload: scheduled

  containers:
    - name: nginx
      image: nginx:latest
      ports:
        - containerPort: 80